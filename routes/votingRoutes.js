const express = require('express')
const votingRoutes = express.Router()

votingRoutes.get('/candidates', async (req, res) => {
    try {
        const candidates = await Candid
    } catch (err) {
        
    }
})